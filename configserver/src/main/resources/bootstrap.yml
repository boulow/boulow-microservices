spring:
  application:
    name: config-server
  profiles:
    active:
    - git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/boulow/boulow-microservices.git
          search-paths:
          - backendProps
    
server:
  port: 8071