spring:
  application:
    name: config-server
  profiles:
    active:
    - git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yguiathe/boulow-config/
          default-label: main
          clone-on-start: true
          skip-ssl-validation: true
          
server:
  port: 8071