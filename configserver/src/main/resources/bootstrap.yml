spring:
  application:
    name: config-server
  profiles:
    active:
    - git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yguiathe/boulow-config.git

server:
  port: 8071